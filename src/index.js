import React from 'react';
//ReactDom is needed to hook to the index.html file in the public folder
import ReactDOM from 'react-dom';

import App from './App';

ReactDOM.render(<App/>, document.getElementById('root'))